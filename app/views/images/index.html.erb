<h1>Gallery</h1>
    <% @images.each do |image| %>
    	<div class="gallery">
      		<%= link_to image_tag(image.link), "images/#{image.id}" %>
      	</div>
    <% end %>
<br>

<%= link_to 'New Image', new_image_path %>
